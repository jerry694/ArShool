import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.belom.wafo.com',
  appName: 'schoolX',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
